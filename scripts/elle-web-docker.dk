FROM ubuntu:latest

WORKDIR "/home"
RUN apt-get update && apt-get install -y\
 git\
 mysql-server\
 python3\
 python3-pip

COPY scripts/pyreqs.txt /scripts/
RUN pip3 install -r /scripts/pyreqs.txt

COPY scripts/clone_all.sh /scripts/
RUN ./scripts/clone_all.sh

#CMD ["bash"]

